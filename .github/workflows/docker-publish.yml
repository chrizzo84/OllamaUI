name: Docker Publish

on:
  push:
    branches: [ main ]
    paths:
      - 'Dockerfile'
      - 'Dockerfile.multi-platform'
      - 'ollama-ui/**'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:
  release:
    types: [published]
  schedule:
    # Run every night at 02:30 UTC
    - cron: '30 2 * * *'

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  # Original mixed-case repo (cannot rely on automatic lowercasing by all tooling)
  IMAGE_NAME_ORIG: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute lowercase image name
        id: names
        run: |
          echo "orig=$IMAGE_NAME_ORIG" >> $GITHUB_OUTPUT
          echo "lower=${IMAGE_NAME_ORIG,,}" >> $GITHUB_OUTPUT
        env:
          IMAGE_NAME_ORIG: ${{ env.IMAGE_NAME_ORIG }}

      - name: Fetch base image digest (ollama/ollama:latest)
        id: base_digest
        run: |
          set -euo pipefail
          docker pull -q ollama/ollama:latest > /dev/null
          # Get canonical digest (repo@sha256:...)
          DIGEST_LINE=$(docker image inspect ollama/ollama:latest --format '{{index .RepoDigests 0}}')
          # Extract just sha256:...
          BASE_DIGEST=${DIGEST_LINE##*@}
          echo "Current base image digest: $BASE_DIGEST"
          echo "digest=$BASE_DIGEST" >> $GITHUB_OUTPUT
      - name: Extract Docker metadata (always run to know actual tags)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ steps.names.outputs.lower }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            # If tag matches v* use semver patterns (produces v1.2.3, v1.2, v1)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
          labels: |
            org.opencontainers.image.source=${{ github.repositoryUrl }}
            org.opencontainers.image.description=Ollama UI + Ollama server combined image
            base.image=ollama/ollama:latest
            base.image.digest=${{ steps.base_digest.outputs.digest }}
            build.triggered.by=${{ github.event_name }}
            commit.sha=${{ github.sha }}

      - name: Derive sha tag reference
        id: sha_ref
        run: |
          set -euo pipefail
          echo "All computed tags (comma separated):"
          echo '${{ steps.meta.outputs.tags }}'
          # Find a tag with :sha-<7hex>
          SHA_TAG=$(echo '${{ steps.meta.outputs.tags }}' | tr ',' '\n' | grep -E ':sha-[0-9a-f]{7}$' | head -n1 || true)
          if [ -z "$SHA_TAG" ]; then
            # Fallback construct (metadata action usually uses short sha)
            SHORT_SHA=$(git rev-parse --short=7 HEAD)
            SHA_TAG="ghcr.io/${{ steps.names.outputs.lower }}:sha-${SHORT_SHA}"
            echo "Fallback sha tag reference constructed: $SHA_TAG"
          else
            echo "Detected sha tag from metadata: $SHA_TAG"
          fi
          echo "image_ref=$SHA_TAG" >> $GITHUB_OUTPUT

      - name: Skip check (all events) - skip if same commit & base image digest
        id: skip_check
        run: |
          set -euo pipefail
          IMAGE='${{ steps.sha_ref.outputs.image_ref }}'
          CUR_BASE_DIGEST='${{ steps.base_digest.outputs.digest }}'
          echo "Evaluating whether to skip build for event: $GITHUB_EVENT_NAME"
          echo "Candidate commit SHA: $(git rev-parse --short=7 HEAD)"
            
          NEED_BUILD=true
          if [ -z "$IMAGE" ]; then
            echo "No image reference derived; will build."
          elif docker manifest inspect "$IMAGE" > /dev/null 2>&1; then
            echo "Image $IMAGE already exists. Checking base image digest label..."
            docker pull -q "$IMAGE" > /dev/null 2>&1 || true
            EXISTING_BASE_DIGEST=$(docker inspect -f '{{ index .Config.Labels "base.image.digest"}}' "$IMAGE" 2>/dev/null || echo 'unknown')
            echo "Current base digest: $CUR_BASE_DIGEST"
            echo "Existing base digest: $EXISTING_BASE_DIGEST"
            if [ "$EXISTING_BASE_DIGEST" = "$CUR_BASE_DIGEST" ] && [ -n "$EXISTING_BASE_DIGEST" ] && [ "$EXISTING_BASE_DIGEST" != "unknown" ]; then
              echo "Commit + base digest unchanged -> skip build & push"
              NEED_BUILD=false
            else
              echo "Base digest differs/missing -> rebuild"
            fi
          else
            echo "No existing image manifest for $IMAGE"
            echo "Attempting relevance-based skip (only run if no relevant files changed and latest image base digest matches)."
            # Determine changed files vs previous commit (simple heuristic; merge commits may differ)
            if git rev-parse HEAD^ >/dev/null 2>&1; then
              git diff --name-only HEAD^ HEAD > /tmp/changed_files.txt
            else
              echo "Single commit repository state; cannot diff -> build required.";
              CHANGED_FORCE_BUILD=1
            fi
            echo "Changed files:"
            cat /tmp/changed_files.txt || true
            # Relevant path patterns (regex for grep -E)
            RELEVANT_PATTERNS='^(Dockerfile(\\.multi-platform)?|build.sh|ollama-ui/|Scraper/|Dockerfile|Dockerfile.multi-platform)'
            # Explicitly exclude README changes from triggering a build
            EXCLUDE_PATTERNS='^(README(\\..*)?)$'
            if [ -f /tmp/changed_files.txt ]; then
              grep -Ev "$EXCLUDE_PATTERNS" /tmp/changed_files.txt > /tmp/changed_files.filtered || true
            fi
            echo "Filtered (non-excluded) changed files (README.* removed):"
            cat /tmp/changed_files.filtered || true
            if [ -z "${CHANGED_FORCE_BUILD:-}" ]; then
              TARGET_LIST=/tmp/changed_files.filtered
              # If filter file empty fall back to original to avoid empty grep edge cases
              if [ ! -s "$TARGET_LIST" ]; then TARGET_LIST=/tmp/changed_files.txt; fi
              if grep -E "$RELEVANT_PATTERNS" "$TARGET_LIST" >/dev/null 2>&1; then
                echo "Relevant changes detected -> build required"
              else
                echo "No relevant path changes. Checking latest image for equivalence."
                LATEST_IMAGE="ghcr.io/${{ steps.names.outputs.lower }}:latest"
                if docker manifest inspect "$LATEST_IMAGE" > /dev/null 2>&1; then
                  docker pull -q "$LATEST_IMAGE" > /dev/null 2>&1 || true
                  LATEST_BASE_DIGEST=$(docker inspect -f '{{ index .Config.Labels "base.image.digest"}}' "$LATEST_IMAGE" 2>/dev/null || echo 'unknown')
                  LATEST_COMMIT_SHA=$(docker inspect -f '{{ index .Config.Labels "commit.sha"}}' "$LATEST_IMAGE" 2>/dev/null || echo 'unknown')
                  echo "Latest image base digest: $LATEST_BASE_DIGEST"
                  echo "Latest image commit sha: $LATEST_COMMIT_SHA"
                  echo "Current commit sha: ${GITHUB_SHA}"
                  if [ "$LATEST_BASE_DIGEST" = "$CUR_BASE_DIGEST" ] && [ "$LATEST_BASE_DIGEST" != "unknown" ]; then
                    echo "Base digest unchanged and no relevant changes -> skip build"
                    NEED_BUILD=false
                  else
                    echo "Base digest differs (or unknown) -> build required"
                  fi
                else
                  echo "Latest tag not present -> build required"
                fi
              fi
            fi
          fi
          if [ "$NEED_BUILD" = true ]; then echo "skip=false" >> $GITHUB_OUTPUT; else echo "skip=true" >> $GITHUB_OUTPUT; fi

      - name: Show decision context
        run: |
          echo "Skip flag: ${{ steps.skip_check.outputs.skip }}"
          echo "SHA image ref: ${{ steps.sha_ref.outputs.image_ref }}"
          echo "Base digest: ${{ steps.base_digest.outputs.digest }}"

      - name: Guard (stop early if skipping)
        if: steps.skip_check.outputs.skip == 'true'
        run: |
          echo "Skipping build & push steps."

      - name: Build and push (single-arch amd64)
        if: steps.skip_check.outputs.skip != 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summarize
        run: |
          if [ "${{ steps.skip_check.outputs.skip }}" = "true" ]; then
            echo "Build skipped: existing image for commit with identical base digest." >> $GITHUB_STEP_SUMMARY
          else
            echo "Published (lowercase) image: ghcr.io/${{ steps.names.outputs.lower }}" >> $GITHUB_STEP_SUMMARY
            echo "Tags:" >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.meta.outputs.tags }}" | tr ',' '\n' >> $GITHUB_STEP_SUMMARY
            echo "\nHint: Ensure the container package visibility is Public under Settings > Packages if pulls return 403." >> $GITHUB_STEP_SUMMARY
          fi
